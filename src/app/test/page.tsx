{/*"use client";
import React, { useState } from "react";
import CreateJobModal from "../components/CreateJobModal/CreateJobModal";;
import { SetUserName, setUserType } from "../server/services/DatabaseService";
import AuthService from "../services/AuthService";
import UserType from "../enums/UserType.enum";
import { Login } from "../Navigation/navigation";
import { useRouter } from "next/navigation";


const TestPage = () => {
  return (
    <section className="min-h-screen flex items-center justify-center bg-gray-100 p-8">
      <article className="bg-white rounded-2xl shadow-lg p-8 w-full max-w-2xl">
        <h1 className="text-2xl font-bold mb-6">Test Job Modal</h1>
        <CreateJobModal />
      </article>
    </section>
  );
};

export default function test(){
    return (
        <>
        <section className="min-h-screen flex items-center justify-center bg-gray-100 p-8">
          <article className="bg-white rounded-2xl shadow-lg p-8 w-full max-w-2xl">
            <h1 className="text-2xl font-bold mb-6">Test Job Modal</h1>
            <CreateJobModal />
          </article>
        </section>
        </>
      );

};*/}
